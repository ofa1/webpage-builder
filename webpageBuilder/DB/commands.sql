create database webpageBuilder;
use webpagebuilder;

create table webpage (PageID numeric, Username varchar(20), title varchar(20), description varchar(1000), paragraphs text, 
						formID varchar(), images varchar(200))